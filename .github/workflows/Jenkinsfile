
pipeline {
     agent any
     environment {
     DOCKERHUB_CREDENTIALS = credentials('docker-hub-nicolas')
     }

    stages {
    	    
     	stage('Build docker image') {
       	    steps{
      	        sh 'docker build -t web-chat/nodeapp:$BUILD_NUMBER ./app'
     	    }
     	}
          
     	stage('login') {
       	    steps{
      	        sh 'docker login -u 448986393877782883328382439534'
                   sh 'dckr_pat_KqjNXgc64IHifahCq6LAw40eMj4'
     	    }
     	}
          
    }	     	
}
