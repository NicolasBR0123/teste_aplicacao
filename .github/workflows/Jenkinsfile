pipeline {
    agent any

    stages {
        stage ('Build Image') {
           steps {
	       script {
		   dockerapp = docker.build("web-chat-test:nodalpine3.15", '-f ./app/Dockerfile ./app')

	      }	
             
           }
      
        }
    }  	
}
