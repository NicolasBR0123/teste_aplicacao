pipeline {
    agent any

    stages {
        stage ('Build Image') {
           steps {
	       script{
		   dockerapp = docker.build("web-chat-test", '-f ./app/Dockerfile ./app')

	      }	
              '
           }
      
        }
    }  	
}
